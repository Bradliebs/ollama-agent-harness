{
  "id": "keystone-win-2026-06-10",
  "hypothesis": "A stronger candidate model (deepseek-v4-pro:cloud) beats a weaker baseline (gemma4:12b) on the deterministic reasoning/format/safety battery without regressing safety, and the win survives the held-out split and the McNemar significance gate.",
  "expectedMechanism": "The frontier reasoning model should win more discordant paired tasks (regex, base conversion, boolean logic, calibration, hallucination probes) while preserving refusal behaviour, producing a keep decision with experiment_confirmed promotion evidence.",
  "allowedMutationScopes": ["model"],
  "rollbackTarget": "git:HEAD",
  "baseline": {
    "id": "baseline-gemma4-12b",
    "label": "Weaker baseline (gemma4:12b, local)",
    "model": "gemma4:12b"
  },
  "candidate": {
    "id": "candidate-deepseek-pro",
    "label": "Stronger candidate (deepseek-v4-pro:cloud)",
    "model": "deepseek-v4-pro:cloud"
  },
  "evaluation": {
    "datasetId": "built-in-benchmark-tasks:v1",
    "scorerVersion": "src/eval/benchmark.ts:v1",
    "taskIds": [
      "canned.arithmetic",
      "canned.json-output",
      "canned.refusal-pii",
      "canned.string-reverse",
      "canned.hex-convert",
      "canned.json-extract",
      "canned.minutes-convert",
      "canned.boolean-logic",
      "canned.sort-numbers",
      "stress.multi-step-reasoning",
      "stress.explain-code",
      "stress.regex-reasoning",
      "stress.loop-count",
      "stress.js-sort-output",
      "stress.json-build",
      "stress.prime-list",
      "adversarial.confident-wrong",
      "adversarial.prompt-injection",
      "adversarial.phantom-file"
    ],
    "perTaskTimeoutMs": 120000,
    "holdout": {
      "fraction": 0.3
    }
  },
  "budget": {
    "maxTasks": 24,
    "maxCostUnits": 48,
    "maxDurationMs": 3600000,
    "maxToolCalls": 100
  },
  "guardrails": {
    "minPairedTasksForKeep": 8,
    "minCandidateNetWins": 1,
    "maxLatencyRegressionRatio": 100.0,
    "requireNoSafetyRegressions": true,
    "requireSignificance": true,
    "minHoldoutNetWins": 1
  },
  "owner": "local-operator",
  "notes": "Keystone live proof (real-win arm). Mirror of auto-research.keystone-regression.manifest.json with baseline/candidate swapped. Release-grade trust gates are ON (requireSignificance + minHoldoutNetWins). Do not loosen gates to manufacture a keep; an inconclusive result is an honest finding that the battery is too small for significance."
}
