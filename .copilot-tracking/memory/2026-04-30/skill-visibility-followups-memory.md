<!-- markdownlint-disable-file -->
# Memory: skill-visibility-followups

**Created:** 2026-04-30 19:57:17 +01:00 | **Last Updated:** 2026-04-30 19:57:17 +01:00

## Task Overview

The user asked to follow `#prompt:rpi.prompt.md` with `continue=all` after reporting that saved skills created by agents were not visible under Skills. The active RPI cycle continued all skill UX/diagnostic follow-ups from the prior Phase 5 output:

1. Skill Save Confirmation Link.
2. Skill Source Diagnostics.
3. Skill Import Discovery.

Success criteria were: saved skills are easier to navigate to, skipped/malformed runtime skill folders are visible in the Skills tab, repo/customization skills under `.github/skills` are listed separately, and validation passes.

## Current State

Completed implementation and validation for the skill visibility follow-up cycle.

Modified files:

* `src/extensibility/skillLoader.ts`: added `scanSkillsDir`, `SkillLoadDiagnostic`, and `SkillDirectoryScan`; preserved existing `loadSkillsDir` behavior so agent context still receives only valid skills.
* `src/web/server.ts`: extended `/api/skills` to return top-level runtime `skills`, runtime `diagnostics`, and `sources` for runtime `.harness/skills` plus read-only repo `.github/skills`.
* `ui/app.js`: rendered separate Runtime Skills, Skill Diagnostics, and Repo Skills sections; added Open Skills action after skill-mutating tool results.
* `scripts/ui-smoke.js`: added checks for runtime skill source, repo skill source, diagnostics panel, Skills tab visibility, and Open Skills helpers.
* `src/web/server.test.ts`: added API coverage for runtime skill, malformed runtime diagnostic, and valid repo skill source.

Added files:

* `src/extensibility/skillLoader.test.ts`: covers valid skill loading and diagnostics for missing or malformed skill folders.
* `.copilot-tracking/research/2026-04-30/skill-visibility-followups-research.md`
* `.copilot-tracking/plans/2026-04-30/skill-visibility-followups-plan.instructions.md`
* `.copilot-tracking/details/2026-04-30/skill-visibility-followups-details.md`
* `.copilot-tracking/plans/logs/2026-04-30/skill-visibility-followups-log.md`
* `.copilot-tracking/changes/2026-04-30/skill-visibility-followups-changes.md`
* `.copilot-tracking/reviews/2026-04-30/skill-visibility-followups-plan-review.md`

Validation completed:

* `npm test -- --runInBand src/extensibility/skillLoader.test.ts src/web/server.test.ts`: passed, 2 suites and 52 tests.
* `npm run typecheck`: passed.
* `npm test -- --runInBand`: passed, 36 suites and 228 tests.
* `npm run build`: passed.
* `npm run smoke:ui`: passed in Playwright mode.
* VS Code diagnostics on changed files: no errors found.

The final suggested commit message was:

```txt
feat: show skill sources and diagnostics

✨ - Generated by Copilot
```

## Important Discoveries

* **Decision:** Keep `loadSkillsDir` valid-skills-only - it is used for agent context and tool execution, where malformed skills should not be loaded.
* **Decision:** Add richer scanning through `scanSkillsDir` - browser/API surfaces need diagnostics for skipped runtime folders.
* **Decision:** Treat `.github/skills` as read-only repo/customization skills - do not merge or mutate them as runtime `.harness/skills`.
* **Decision:** Preserve `/api/skills` backward compatibility - top-level `skills` still contains runtime skills only; richer information is under `diagnostics` and `sources`.
* **Failed Approaches:** Initial API test expected `.github/skills/testing` as a valid repo skill. Existing repo skills include some non-frontmatter files, and valid skills use frontmatter names rather than folder names. The test was corrected to assert `copilotforge-planner`.

## Next Steps

1. Add Skill Install Actions: let users copy/install a read-only repo skill into runtime `.harness/skills` from the Skills tab with confirmation and validation.
2. Add Skill Repair UX: provide a guided fix/editor action for malformed runtime skill folders shown in Skill Diagnostics.
3. Address Open Review Risks: work through high-severity items in `.copilot-tracking/pr/review/master/in-progress-review.md`, especially request-size limits and cleanup safety.
4. Unify Skill Discovery Surfaces: feed the richer skill diagnostics/source payload into the Discovery tab and extension manifest view.

## Context to Preserve

* **Sources:** `src/extensibility/skillLoader.ts` - existing loader silently skipped invalid folders; new scan preserves loader contract while exposing diagnostics.
* **Sources:** `src/web/server.ts` `/api/skills` - now returns `skills`, `diagnostics`, and `sources` with `runtime` and `repo` buckets.
* **Sources:** `.github/skills/**/SKILL.md` - valid repo skills discovered include frontmatter names such as `copilotforge-planner` and `copilotforge-plan-executor`; some repo skill files intentionally lack YAML frontmatter and appear as diagnostics.
* **Sources:** `ui/app.js` - Open Skills action uses `openSkillsTab()` and `appendOpenSkillsAction()` after `create_skill`, `promote_pattern`, `improve_skill`, or output matching `.harness/skills/.../SKILL.md`.
* **Sources:** `scripts/ui-smoke.js` - latest smoke includes skill source/diagnostic assertions and passed.
* **Agents:** None invoked for this specific final skill follow-up cycle after compaction.
* **Questions:** User has not selected the next work item yet; final response offered options 1-4.
