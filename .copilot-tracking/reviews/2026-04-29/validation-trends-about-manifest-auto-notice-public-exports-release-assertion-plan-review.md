<!-- markdownlint-disable-file -->
# Review: v0.1.14 — Validation Trend Provenance and Release Assertions

- Plan: `.copilot-tracking/plans/2026-04-29/validation-trends-about-manifest-auto-notice-public-exports-release-assertion-plan.instructions.md`
- Reviewer: RPI Agent
- Date: 2026-04-29
- Commit: 8d7c486 (`feat: add validation trend provenance`)
- Tag: v0.1.14 (pushed)

## User Request Fulfillment

| # | Request | Status | Evidence |
|---|---------|--------|----------|
| 1 | Validation trend drill-down by selection source (auto vs manual) | Complete | `bySelectionSource` summary in `evalTrace.ts`; UI renders `outputValidationSourceTrend`; tests in `evalTrace.test.ts` and `server.test.ts` |
| 2 | About panel manifest link | Complete | `getAboutInfo` returns `manifestUrl`; `renderAboutPanel` adds Manifest row; `server.test.ts` asserts presence |
| 3 | Auto-selection explanation notice in chat | Complete | `/api/chat` emits `output_validation_profile` SSE; UI `appendOutputValidationProfileItem` renders 🧭 notice with reason |
| 4 | Public validation API exports | Complete | `src/index.ts` exports profile templates, suggester, trend export, and types; new test in `outputValidation.test.ts` exercises imports |
| 5 | Release manifest CI assertion | Complete | `scripts/release-smoke.js` adds strict manifest field checks; `release.yml` runs "Smoke release archive and manifest" before publish |

## Validation Outcomes

Local (pre-commit):

- Focused tests: 60/60 pass
- Typecheck: pass
- Full Jest: 152/152 pass
- Build: pass
- UI smoke (static + Playwright modes): pass
- Release smoke (strict manifest assertions): pass

Remote (post-push):

- CI run 25115352616: completed / success
- Release run 25115356049: completed / success — Validate, Package, Generate manifest, Smoke release archive and manifest, Generate notes, Publish release asset, Verify published release asset all succeeded

## Placement and Quality

- Selection source modeled at the trace layer and surfaced consistently through API → SSE → UI.
- Public exports added through the existing `src/index.ts` barrel; no new top-level surface introduced.
- Release assertions live alongside existing smoke logic and are wired into the workflow before publish so a bad manifest fails fast.

## Status

Complete — all five user requests fulfilled, local validation green, remote workflows green.

## Informational commit message (already applied as 8d7c486)

```text
feat: add validation trend provenance

📊 - Generated by Copilot
```
