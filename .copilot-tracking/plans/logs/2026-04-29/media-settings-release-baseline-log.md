<!-- markdownlint-disable-file -->

# Media Settings Release Baseline Planning Log

## Decisions

* Create a new root README because none exists.
* Store media tool settings under `mediaTools` in the existing settings file.
* Apply the persisted values to `process.env` so existing tools can keep reading the same env names.
* Create a private GitHub release baseline after validation and push.

## Status

Planning complete.
